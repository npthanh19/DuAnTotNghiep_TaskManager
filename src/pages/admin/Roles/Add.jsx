import React from 'react';
import { Add } from '../../../sections/admin/roles/Add';

const AddRoles = () => {
     return (
          <div>
               <Add />
          </div>
     );
};

export default AddRoles;
